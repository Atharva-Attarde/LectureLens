# Pdf2Speech dependencies
# Install with:  pip install -r requirements.txt

# PDF text extraction
pymupdf>=1.24.0

# TTS engine (Kokoro — lightweight, high quality, runs on CPU or GPU)
kokoro>=0.9.2

# Audio I/O
soundfile>=0.12.1

# Cover image generation (optional — falls back to solid colour if missing)
pillow>=10.0.0

# Progress bars & Model acceleration
tqdm>=4.65.0
accelerate>=0.26.0

# numpy and torch are required by Kokoro and are pulled in automatically
